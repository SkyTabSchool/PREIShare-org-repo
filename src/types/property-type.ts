/**
 * Closed property-type (asset class) values from the field inventory.
 * Import this alias instead of repeating the union in other files.
 */
export type PropertyType =
  | "multifamily"
  | "office"
  | "retail"
  | "industrial"
  | "mixed_use"
  | "land";
